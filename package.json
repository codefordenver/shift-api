{
  "name": "shift-api",
  "description": "run queries on postgres database in different handlers reponding to different api endpoints",
  "version": "1.0.0",
  "author": "Nikki Zhu",
  "license": "MIT",
  "dependencies": {
    "sequelize": "^4.42.0",
    "pg": "^7.7.1"
  },
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
